package com.phone2pro.camera.core;

/** Exposure scheduling selected before a capture starts. */
public enum ExposureStrategy {
    SINGLE_AUTO,
    SHORT_EXPOSURE_BURST,
    BALANCED_BRACKET,
    LOW_LIGHT_BRACKET,
    DETAIL_BURST
}
