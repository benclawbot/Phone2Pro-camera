package com.phone2pro.camera.capture;

/** Portable request intent mapped by the Android binder to native templates. */
public enum CaptureRequestTemplate {
    PREVIEW,
    STILL_CAPTURE,
    ZERO_SHUTTER_LAG,
    MANUAL,
    VIDEO_RECORD
}
