package com.phone2pro.camera.storage;

/** Metadata fields considered before final publication. */
public enum MetadataField {
    ORIENTATION,
    CAPTURE_TIMESTAMP,
    EXPOSURE_TIME,
    SENSITIVITY_ISO,
    FOCAL_LENGTH,
    LENS_ROUTE,
    CAPTURE_PROFILE,
    SOFTWARE,
    LOCATION,
    DEVICE_MAKE_MODEL,
    DIAGNOSTIC_XMP,
    PROCESSING_XMP
}
