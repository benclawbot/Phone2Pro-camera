package com.phone2pro.camera.core;

/** Describes how the delivered field of view is produced, independently of backend transport. */
public enum RouteRendering {
    OPTICAL,
    IN_SENSOR,
    DIGITAL,
    UNAVAILABLE
}
