package com.phone2pro.camera.ui;

/** User-visible shutter and save feedback, independent from background processing. */
public enum CaptureFeedback {
    READY,
    FOCUSING,
    CAPTURING,
    SAVING,
    SAVED,
    ERROR
}
