# Storage and gallery architecture

The canonical executable contract is documented in [STORAGE_GALLERY_CONTRACTS.md](STORAGE_GALLERY_CONTRACTS.md).

That specification covers pending MediaStore rows, durable-byte publication, failure cleanup, recovery journaling, metadata privacy, thumbnails, and system viewer intents. This stable index exists so capability-matrix and engineering-guide references do not depend on the longer implementation-specific filename.
