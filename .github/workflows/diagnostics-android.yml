name: Diagnostics Android

on:
  push:
    paths:
      - "diagnostics/**"
      - ".github/workflows/diagnostics-android.yml"
  pull_request:
    paths:
      - "diagnostics/**"
      - ".github/workflows/diagnostics-android.yml"

concurrency:
  group: diagnostics-android-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.5.0"
      - name: Lint and build diagnostics APK
        run: gradle -p diagnostics lintDebug assembleDebug
      - name: Upload diagnostics APK
        uses: actions/upload-artifact@v4
        with:
          name: phone2pro-diagnostics-debug
          path: diagnostics/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14
